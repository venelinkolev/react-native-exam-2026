export type UserProfile = {
    username: string;
    fullName: string;
    birthDate: string;
    city: string;
    street: string;
    postCode: string;
};

export type UserProfileForm = UserProfile;